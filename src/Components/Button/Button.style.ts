import { StyleSheet } from 'react-native';
const ButtonStyles = StyleSheet.create({
  text: {
    fontSize: 16,
    textAlign: 'center',
    fontWeight: '500',
  },
  view: {
    alignSelf: 'center',
    width: '60%',
  },
});
export default ButtonStyles;
